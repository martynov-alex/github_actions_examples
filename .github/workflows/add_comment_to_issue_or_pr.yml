name: Add comment to issue or PR

on:
  workflow_call:
    inputs:
      # Номер issue или PR
      ISSUE_NUMBER:
        type: string
        required: true
      # Текст комментария
      COMMENT_TEXT:
        type: string
        required: true
      # GitHub реакция: +1, -1, laugh, confused, heart, hooray, rocket, eyes
      REACTIONS:
          type: string
          required: true

jobs:
  add_comment:
    name: Add comment to issue or PR
    runs-on: ubuntu-latest
    steps:
      - name: Create comment
        uses: peter-evans/create-or-update-comment@v4
        with:
          issue-number: ${{ inputs.ISSUE_NUMBER }}
          body: ${{ inputs.COMMENT_TEXT }}
          reactions: ${{ inputs.REACTIONS }}

